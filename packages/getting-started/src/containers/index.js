import CounterApp from './CounterApp';
import FormApp from './FormApp';
import ListApp from './ListApp';

const apps = {
  counter: CounterApp,
  form: FormApp,
  list: ListApp,
};

export default apps;
